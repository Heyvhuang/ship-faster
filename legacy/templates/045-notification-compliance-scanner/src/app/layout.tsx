import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NotifyScan — Notification Compliance Scanner for iOS Apps",
  description:
    "Automated notification compliance scanner for iOS apps. Audit against GDPR, CCPA, and Apple notification permission requirements in seconds.",
  openGraph: {
    title: "NotifyScan — Notification Compliance Scanner",
    description:
      "Scan iOS apps for notification permission compliance across GDPR, CCPA, and Apple guidelines.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-zinc-950 text-zinc-100`}
      >
        {children}
      </body>
    </html>
  );
}

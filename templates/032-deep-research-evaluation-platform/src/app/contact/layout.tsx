import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Request a Demo — DREAM Eval",
  description:
    "Get a pilot evaluation of your AI research agent within 48 hours. Contact the DREAM Eval team.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
